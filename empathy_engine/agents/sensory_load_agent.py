class SensoryLoadAgent:

    def run(self, interaction: str) -> dict:
        return {
            "possible_factors": [
                "noise",
                "time pressure"
            ]
        }
